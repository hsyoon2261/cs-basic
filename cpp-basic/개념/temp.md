네트워크

# NAT

IP 패킷의 tcp/udp 포트 숫자와 소스 및 목적지의 ip주소 등을 재기록하면서 라우터를 통해 네트워크 트래픽을 주고 받는 기술

원래 받았던 특정 ip주소 /포트번호로 가는 패킷을 다른 주소에 다른포트로 바꿔주는  

이걸로 사설ip 와 공인 ip를 바꿔주는데 쓴다 

패킷에 변화가 생김 => 체크섬도 다시 계산해서 재기록 해야함. 

# 포트포워딩

NAT를 응용한 기술 = > 패킷이 라우터나 방화벽과 같은 네트워크 장비를 가로지르는 동안 특정 ip주소와 포트 번호의 통신 요청을 특정 다른 ip와 포트 번호로 넘겨주는 네트워크 주소 변환(NAT)의 응용이다

게이트웨이(외부망) 의 반대쪽에 위치한 사설네트워크에 상주하는 호스트에 대한 서비스를 생성하기 위해 사용

Eth IPv4 TCP DATA(500)

# c++ 
## 컴파일러가 자동으로 생성해주는 함수
생성자 소멸자 복사생성자 복사대입연산자
생성자 소멸자는 따로 명시하지 않아도 알아서 생성된다.
따로 만들어 줄수도 있음
